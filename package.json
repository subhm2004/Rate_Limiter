{
  "name": "rate-limiter",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack rate limiter — Node.js server with C++ (N-API) algorithms + Next.js frontend, nine algorithms.",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "npm install --prefix backend",
    "build:frontend": "npm install --prefix frontend && npm run build --prefix frontend",
    "backend": "npm install --prefix backend && node backend/server.js",
    "frontend": "npm run dev --prefix frontend",
    "test": "make -C backend test",
    "clean": "make -C backend clean && rm -rf frontend/.next backend/build backend/node_modules frontend/node_modules"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
