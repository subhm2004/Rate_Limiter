{
  "name": "rate-limiter-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js (Express) server whose rate-limiting algorithms are implemented in C++ and loaded as an N-API native addon.",
  "main": "server.js",
  "gypfile": true,
  "scripts": {
    "build": "node-gyp rebuild",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "node-addon-api": "^8.3.0"
  },
  "devDependencies": {
    "node-gyp": "^11.2.0"
  }
}
